"use client"

import ProjectsDisplay from "../../src/components/ProjectsDisplay";

export default function ProjectsPage() {
  // This page component now acts as a container or entry point.
  // It might handle fetching page-level data or layout in a real app.
  return (
    <ProjectsDisplay />
  );
}
